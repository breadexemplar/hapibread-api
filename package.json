{
  "name": "hapibread-api",
  "version": "0.1.0",
  "author": "Gene Diaz <genediazjr@gmail.com> (http://genediazjr.com)",
  "description": "Hapi BREAD API",
  "repository": "git://github.com/breadexemplar/hapibread-api",
  "bugs": "https://github.com/breadexemplar/hapibread-api/issues",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=4.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "test": "lab -c -v -L -a code",
    "test-cover": "lab -c -v -L -a code -r html -o ./coverage.html && open ./coverage.html"
  },
  "dependencies": {
    "hapi": "15.0.x",
    "boom": "4.0.x",
    "joi": "9.0.x",
    "pg": "6.1.x",
    "pg-native": "1.10.x",
    "proxyquire": "1.7.x"
  },
  "devDependencies": {
    "coveralls": "2.11.x",
    "code": "3.0.x",
    "lab": "11.0.x"
  }
}
